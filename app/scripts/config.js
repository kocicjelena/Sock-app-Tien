/*global define */
define([''], function () {
    'use strict';

    var config = {};

    config.getRootUrl = function() {
        //return 'http://';
        return 'http://localhost:3000'
    }

    return config;
});